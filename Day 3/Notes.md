Attributess


1> setAttribute (Attribute name, vale )
2> getAttribute ('AN',)

3> removeAttribute('AN')
4> hasAttribute('AN') BOOLEAN IS IT = TRUE, IS IT = FALSE


HTML only validates the default attributes

custon attributes --> data-Attribute name (HTML --> valid)



creating inserting and removing elements from DOM

1. createElement

2. Insertation

old api (still runs everywhere)


i) appendchild -- old / insertBefore()  / removechild()

Modern API know both

ii) append -- new - multiple nodes + string in one call
--> prepend()  / before() / after() / replacewith()