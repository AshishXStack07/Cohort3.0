Event delegation---> where a single event listener is attached to a parent element to handle events for all its child elements, rather than attaching individuals listeners to each child.

global event


set interval---- hamesa chal ta rehta hai
set timeout --ek bar chalta hai